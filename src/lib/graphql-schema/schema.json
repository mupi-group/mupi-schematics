{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsMupiGraphqlSchema",
  "title": "Mupi GraphqlSchema Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the graphql schema.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name of graphql schema would you like to generate?"
    },
    "path": {
      "type": "string",
      "description": "The path to generate the graphql type."
    },
    "items": {
      "type": "string",
      "description": "The items to generate the graphql type."
    },
    "graphqlSchemaItems": {
      "type": "string",
      "description": "The graphql formatted items to generate the graphql type."
    },
    "graphqlSchemaInputItems": {
      "type": "string",
      "description": "The graphql formatted input items to generate the graphql type."
    },
    "graphqlSchemaIDPropertyKey": {
      "type": "string",
      "description": "The graphql update id property items to generate the graphql type."
    }
  },
  "required": ["name"]
}
