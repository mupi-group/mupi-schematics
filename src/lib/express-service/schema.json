{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsMupiExpressService",
  "title": "Mupi ExpressService Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the graphql schema.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name of graphql schema would you like to generate?"
    },
    "path": {
      "type": "string",
      "description": "The path to generate the graphql type."
    },
    "items": {
      "type": "string",
      "description": "The items to generate the graphql type."
    },
  },
  "required": ["name"]
}
